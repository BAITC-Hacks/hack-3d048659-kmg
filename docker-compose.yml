services:
  web:
    image: vetroprognoz:local
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "${BIND_ADDRESS:-127.0.0.1}:${PORT:-8088}:8080"
    restart: unless-stopped
